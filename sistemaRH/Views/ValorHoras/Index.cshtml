@model IEnumerable<sistemaRH.Models.ValorHora>

@{
    ViewData["Title"] = "Index";
}
<style>

    body {
        font-family: Roboto, Helvetica, sans-serif;
        background-color: #f4f4f4;
        background-size: cover;
        background-position: center center;
        background-repeat: no-repeat;
        margin: auto;
        color: black;
    }

</style>

<h1>Valor da Hora</h1>

<p>
    <a asp-action="Create">Registrar valor</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Valor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Valor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdValorHora">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdValorHora">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdValorHora">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
